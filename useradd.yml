##################################################
---
- hosts: centos
  vars:
    - username: hoge
  tasks:
    - name: Add User
      user: name={{ username }}
      become: yes
##################################################
